# sum of two integers
# Define a method (sum) that takes two integers as arguments
# Assign a result variable to the sum of the two integers
# Puts result

# a method that takes an array of strings, and returns a string that is all those strings concatenated together
# Define a method (string_combo) that takes an array as an argument
# Take the first string in the array, and set it to it's own string
# Iteratively take each string after the first and add it to the first string
# Puts the string

# a method that takes an array of integers, and returns a new array with every other element
# Define a method (other_integer) that takes an array as an argument
# Set a counter to zero that increments every iteration of the array
# When the counter is even, it will add the integer to a new array 
# Puts the new array
